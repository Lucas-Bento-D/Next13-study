import React from "react";

const LayoutShop = ({children}: {children: React.ReactNode} ) => {
    return(
        <>
        "Layout Shop"
        {children}
        </>
    )
}
export default LayoutShop